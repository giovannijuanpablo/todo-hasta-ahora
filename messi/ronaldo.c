#include <stdio.h>

int main(int argc, char  *argv[]) {

 char rta = argv[1][0];
 if (rta == 'y') {
   printf("si\n", );

} else if (rta == 'n'){
    printf("no\n", );
 } else {
   printf("respuesta invalida\n", );
 }
  a
  return 0;
}
